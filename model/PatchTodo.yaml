---
params:
  id:
    base: $id
    filter: Item::load
  title:
    base: $title
    optional: true
  completed:
    base: $completed
    optional: true
  order:
    base: $order
    optional: true
model: Item::update
